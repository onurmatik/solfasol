# Extractable Components

No reusable React/Vue components exist in this Django template project.

Potential conceptual components for Superdesign only:

- `Topbar` from `templates/base.html`
- `OfferCard` from `templates/coop/dashboard.html`
- `AgendaItem` from `templates/calendar/agenda.html`

These are template snippets rather than standalone component source files, so skip Superdesign component extraction for now.
